# Session tracking: n8n webhook URL for session snapshot moments (POST JSON).
# Only used when a snapshot event is detected (session_start, analysis_start, analysis_complete, contact_capture).
# Example: https://your-instance.app.n8n.cloud/webhook-test/session-info
N8N_WEBHOOK_URL_SESSION=

# Analysis: n8n webhook when user clicks Analyze (POST JSON payload with analysis_id, session_id, images, etc.)
N8N_WEBHOOK_URL_ANALYZE=

# Cloudinary (client-side direct upload; UUID used as public_id)
# Cloud name from Cloudinary dashboard (e.g. your-cloud-name). Used to build upload URL.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# Upload preset name (e.g. wiser-pet-food-input). Must be an unsigned preset for client upload.
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
