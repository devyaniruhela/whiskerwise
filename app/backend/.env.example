# Copy to .env and fill in — D maintains the real .env; values are never read by Claude.
GEMINI_API_KEY=
DATABASE_URL=            # Supabase direct Postgres connection string (Phase 5)
SUPABASE_URL=            # identity/session only — data access is direct Postgres
SUPABASE_ANON_KEY=
# WISER_CONFIG=          # optional: override path to app/config.yaml
