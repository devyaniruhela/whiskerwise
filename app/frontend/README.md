# Wiser frontend — placeholder

Next.js 15 app lands here in Phase 6 (see build plan). Assets are copied out of
`past work/wiser-by-whisker-wise/` per `old-code-context.md` §11; page-level
components are rebuilt fresh. Do not modify `past work/` itself.
